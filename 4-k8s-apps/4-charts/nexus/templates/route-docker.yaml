apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: nexus
  name: docker
spec:
  host: {{ .Values.web.route.docker }}
  port:
    targetPort: 8082
  to:
    kind: ""
    name: nexus
  tls:
    insecureEdgeTerminationPolicy: None
    termination: edge
