spring:
  application:
    name: api-gateway
  cloud:
    kubernetes:
      secrets:
        paths: /etc/secrets
      config:
        name: api-gateway
        sources:
          - name: api-gateway
