replicaCount: 1

web:
  name: api-gateway
  image:
    repository: docker.apps.openshift-1.blierop.com/api-gateway
    tag: latest
    pullPolicy: IfNotPresent
  enviroment: prod
  ingress:
    issuer: letsencrypt-prod
    domain: example.apps.openshift-1.blierop.com
    secretName: cg-demo-com-tls
    path: /
    serviceMesh:
      enabled: true
service:
  type: ClusterIP
  port: 8080
serviceAccountName: example-app

secrets: {}

