apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: jenkins
  name: jenkins
  namespace: cicd
spec:
  host: jenkins.apps.adriaanmolendijk.info
  port:
    targetPort: 8080
  to:
    kind: Service
    name: jenkins
  tls:
    insecureEdgeTerminationPolicy: None
    termination: edge